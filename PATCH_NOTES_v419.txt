GEJAST paardenrace patch v419

GitHub zip files:
- paardenrace.html
- gejast-config.js
- PATCH_NOTES_v419.txt

Separate SQL:
- gejast_v419_paardenrace.sql

Changes:
- bumped visible version to v419
- empty rooms are cleaned up automatically
- host/admin role is reassigned if the recorded host is no longer present
- when a player joins a room that has no active host present, that player becomes host
- wagers are now locked per player per lobby once set
- a player can ready/unready with the same wager but cannot change the amount afterward
