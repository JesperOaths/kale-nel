import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "npm:@supabase/supabase-js@2.57.4";

const PRINTIFY_BASE = "https://api.printify.com/v1";
const TIKKIE_URL = "https://api.abnamro.com/v2/tikkie/paymentrequests";
const MAX_ITEMS = 20;
const MAX_QTY = 10;
const ALLOWED_ORIGINS = new Set(["https://kalenel.nl", "https://www.kalenel.nl", "https://jesperoaths.github.io"]);
const text = (v) => String(v ?? "").trim();
const money = (cents) => `€${(Number(cents || 0) / 100).toFixed(2)}`;
const clean = (v) => text(v).replace(/\s+/g, " ");
function cors(req) { const origin=text(req.headers.get("origin")); const allow=ALLOWED_ORIGINS.has(origin)||/^http:\/\/(?:localhost|127\.0\.0\.1)(?::\d+)?$/.test(origin)?origin:"https://kalenel.nl"; return {"Access-Control-Allow-Origin":allow,"Vary":"Origin","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"GET, POST, OPTIONS","Content-Type":"application/json; charset=utf-8","Cache-Control":"no-store"}; }
function json(req,body,status=200){return new Response(JSON.stringify(body),{status,headers:cors(req)});}
function normalizeCountry(v){return text(v||"NL").toUpperCase().replace(/[^A-Z]/g,"").slice(0,2);}
function splitName(full){const parts=clean(full).split(" ").filter(Boolean);return{first_name:parts[0]||"Customer",last_name:parts.slice(1).join(" ")||parts[0]||"Customer"};}
function sizeFromVariant(v){const m=clean(v?.title).toLowerCase().match(/(?:^|\s|\/|\|)(xs|s|m|l|xl|2xl|3xl|4xl|5xl)(?:$|\s|\/|\|)/i);return m?m[1].toUpperCase():"";}
async function sha256(value){const d=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(value));return[...new Uint8Array(d)].map(b=>b.toString(16).padStart(2,"0")).join("");}
async function printify(token,path,init={}){const res=await fetch(`${PRINTIFY_BASE}${path}`,{...init,headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json","User-Agent":"Kalenel-Manual-Shop/8.25",...(init.headers||{})}});const raw=await res.text();let payload=null;try{payload=raw?JSON.parse(raw):null}catch{payload=raw}if(!res.ok)throw new Error(`Printify ${res.status}: ${text(payload?.message||payload?.error||raw).slice(0,350)}`);return payload;}
async function resolvePrintifyToken(supabase){const envToken=text(Deno.env.get("PRINTIFY_API_TOKEN"));if(envToken)return envToken;const{data,error}=await supabase.rpc("get_printify_api_token_v815a");if(error||!text(data))throw new Error("Printify token unavailable");return text(data);}
async function selectShop(token){const shops=await printify(token,"/shops.json");const configured=text(Deno.env.get("PRINTIFY_SHOP_ID"));const rows=Array.isArray(shops)?shops:[];if(configured){const found=rows.find(s=>String(s?.id)===configured);if(found)return found;}return rows.find(s=>/shopify/i.test(text(s?.sales_channel)))||rows.find(s=>text(s?.sales_channel).toLowerCase()!=="disconnected")||rows[0]||null;}
async function loadPrintifyProducts(token,shopId){const out=[];for(let page=1;page<=100;page++){const p=await printify(token,`/shops/${shopId}/products.json?limit=50&page=${page}`);const rows=Array.isArray(p?.data)?p.data:[];out.push(...rows);const last=Number(p?.last_page||0);if(!rows.length||rows.length<50||(last&&page>=last))break;}return out;}
function findVariant(products,item){const sku=text(item?.sku);if(sku){for(const p of products){const v=(Array.isArray(p?.variants)?p.variants:[]).find(x=>text(x?.sku)===sku&&x?.is_enabled!==false);if(v)return{product:p,variant:v};}}const name=clean(item?.name).toLowerCase(),size=clean(item?.size).toUpperCase();const product=products.find(p=>clean(p?.title).toLowerCase()===name);if(!product)return null;const variants=(Array.isArray(product?.variants)?product.variants:[]).filter(v=>v?.is_enabled!==false);const variant=variants.find(v=>sizeFromVariant(v)===size)||variants.find(v=>clean(v?.title).toUpperCase().includes(size))||variants[0];return variant?{product,variant}:null;}
function validEmail(email){return/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)&&email.length<=254;}
function validPaymentUrl(raw,provider){if(!text(raw))return"";try{const u=new URL(raw);if(u.protocol!=="https:")return"";const host=u.hostname.toLowerCase();if(provider==="bunq_me"&&(host==="bunq.me"||host.endsWith(".bunq.me")))return u.toString();if(provider==="tikkie"&&(host==="tikkie.me"||host.endsWith(".tikkie.me")))return u.toString();}catch{}return"";}
async function createPaymentRequest(settings,totalCents,reference){const provider=text(settings?.provider||"manual_transfer");if(provider==="tikkie"){const apiKey=text(Deno.env.get("TIKKIE_API_KEY")),appToken=text(Deno.env.get("TIKKIE_APP_TOKEN"));if(apiKey&&appToken){const expiry=new Date(Date.now()+7*86400000).toISOString().slice(0,10);const res=await fetch(TIKKIE_URL,{method:"POST",headers:{"API-Key":apiKey,"X-App-Token":appToken,"Content-Type":"application/json"},body:JSON.stringify({amountInCents:totalCents,description:`Bruis order ${reference}`.slice(0,35),expiryDate:expiry,referenceId:reference})});const raw=await res.text();let p={};try{p=raw?JSON.parse(raw):{}}catch{}if(res.ok&&text(p?.url))return{provider:"tikkie",url:text(p.url),token:text(p.paymentRequestToken),expires_at:p.expiryDate?`${p.expiryDate}T23:59:59Z`:null,generated:true};console.error("Tikkie payment request failed",res.status,raw.slice(0,300));}}const configuredUrl=validPaymentUrl(settings?.payment_url,provider);return{provider,url:configuredUrl,token:"",expires_at:null,generated:false};}
function htmlEscape(v){return text(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));}
async function sendEmail(to,subject,html,plain){const key=text(Deno.env.get("RESEND_API_KEY")),from=text(Deno.env.get("RESEND_FROM_EMAIL"));if(!key||!from)return{ok:false,skipped:true,error:"Resend not configured"};const res=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify({from,to:[to],subject,html,text:plain})});const raw=await res.text();return res.ok?{ok:true,response:raw}:{ok:false,skipped:false,error:`Resend ${res.status}: ${raw.slice(0,300)}`};}
Deno.serve(async(req)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:cors(req)});if(req.method==="GET")return json(req,{ok:true,mode:"manual-payment-v825",creates_orders:false});if(req.method!=="POST")return json(req,{error:"method_not_allowed"},405);const supabaseUrl=Deno.env.get("SUPABASE_URL"),serviceKey=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");if(!supabaseUrl||!serviceKey)return json(req,{error:"server_not_configured"},503);const supabase=createClient(supabaseUrl,serviceKey,{auth:{persistSession:false,autoRefreshToken:false}});try{const body=await req.json();if(text(body?.website))return json(req,{error:"invalid_request"},400);const customer=body?.customer||{},fullName=clean(customer?.name),email=text(customer?.email).toLowerCase(),phone=text(customer?.phone).slice(0,40),address1=clean(customer?.address1),address2=clean(customer?.address2).slice(0,100),city=clean(customer?.city),region=clean(customer?.region).slice(0,100),zip=clean(customer?.zip).slice(0,24),country=normalizeCountry(customer?.country),items=Array.isArray(body?.items)?body.items:[],checkoutKey=text(body?.checkout_idempotency_key),confirmationToken=text(body?.confirmation_token);if(fullName.length<2||fullName.length>120||!validEmail(email)||!address1||!city||!zip||country.length!==2)return json(req,{error:"invalid_customer_or_address"},400);if(!items.length||items.length>MAX_ITEMS)return json(req,{error:"invalid_cart"},400);if(!/^[A-Za-z0-9_-]{20,100}$/.test(checkoutKey)||confirmationToken.length<32||confirmationToken.length>200)return json(req,{error:"invalid_checkout_token"},400);const tokenHash=await sha256(confirmationToken);const{data:existing}=await supabase.from("shop_orders").select("id,status,total_cents,payment_reference,payment_provider,payment_request_url,payment_request_expires_at,confirmation_token_hash").eq("checkout_idempotency_key",checkoutKey).maybeSingle();if(existing){if(text(existing.confirmation_token_hash)!==tokenHash)return json(req,{error:"idempotency_conflict"},409);return json(req,{ok:true,order_id:existing.id,status:existing.status,total_cents:existing.total_cents,payment_reference:existing.payment_reference,payment_provider:existing.payment_provider,payment_url:existing.payment_request_url||"",payment_expires_at:existing.payment_request_expires_at||null,confirmation_token:confirmationToken,replayed:true});}const printifyToken=await resolvePrintifyToken(supabase),shop=await selectShop(printifyToken);if(!shop?.id)throw new Error("No Printify shop available");const shopId=Number(shop.id),products=await loadPrintifyProducts(printifyToken,shopId),authoritative=[];let subtotalCents=0;for(const raw of items){const qty=Math.max(1,Math.min(MAX_QTY,Math.floor(Number(raw?.qty||0))));if(!Number.isFinite(qty)||qty<1)throw new Error("Invalid quantity");const found=findVariant(products,raw);if(!found)throw new Error(`Product variant unavailable: ${clean(raw?.name)} ${clean(raw?.size)}`);const unit=Math.round(Number(found.variant?.price));if(!Number.isFinite(unit)||unit<=0)throw new Error(`Invalid authoritative price: ${clean(raw?.name)}`);subtotalCents+=unit*qty;authoritative.push({name:clean(found.product?.title),size:clean(raw?.size||sizeFromVariant(found.variant)),sku:text(found.variant?.sku),qty,unit_price_cents:unit,printify_product_id:text(found.product?.id),printify_variant_id:Number(found.variant?.id),image:text(raw?.image),collection:text(raw?.collection)});}if(subtotalCents<=0||subtotalCents>1000000)throw new Error("Order total outside allowed range");const nm=splitName(fullName),addressTo={...nm,email,phone,country,region,address1,address2,city,zip},pfLineItems=authoritative.map((i,idx)=>({product_id:i.printify_product_id,variant_id:i.printify_variant_id,quantity:i.qty,external_id:`${checkoutKey}-${idx+1}`.slice(0,100)}));const shippingQuote=await printify(printifyToken,`/shops/${shopId}/orders/shipping.json`,{method:"POST",body:JSON.stringify({line_items:pfLineItems,address_to:addressTo})});let shippingMethod="standard",shippingMethodCode=1,shippingCents=Number(shippingQuote?.standard);if(!Number.isFinite(shippingCents)){shippingMethod="economy";shippingMethodCode=4;shippingCents=Number(shippingQuote?.economy);}if(!Number.isFinite(shippingCents)){shippingMethod="priority";shippingMethodCode=2;shippingCents=Number(shippingQuote?.priority??shippingQuote?.express);}if(!Number.isFinite(shippingCents)||shippingCents<0)throw new Error("No shipping method available for this address");shippingCents=Math.round(shippingCents);const totalCents=subtotalCents+shippingCents,orderId=crypto.randomUUID(),reference=`BRUIS-${orderId.replaceAll("-","").slice(0,8).toUpperCase()}`;const{data:settings}=await supabase.from("shop_payment_settings").select("provider,payment_url,enabled,payment_instructions").eq("id",1).maybeSingle();const payment=settings?.enabled===false?{provider:"manual_transfer",url:"",token:"",expires_at:null,generated:false}:await createPaymentRequest(settings||{},totalCents,reference);const row={id:orderId,status:"pending",currency:"eur",subtotal_cents:subtotalCents,shipping_cents:shippingCents,tax_cents:0,discount_cents:0,total_cents:totalCents,shipping_method:shippingMethod,shipping_method_code:shippingMethodCode,line_items:authoritative,shipping_address:addressTo,customer_email:email,customer_name:fullName,customer_phone:phone||null,payment_provider:payment.provider||"manual_transfer",payment_reference:reference,payment_request_token:payment.token||null,payment_request_url:payment.url||null,payment_request_expires_at:payment.expires_at,confirmation_token_hash:tokenHash,checkout_idempotency_key:checkoutKey,printify_shop_id:shopId};const{error:insertError}=await supabase.from("shop_orders").insert(row);if(insertError)throw insertError;const payLine=payment.url?`<p><a href="${htmlEscape(payment.url)}" style="display:inline-block;background:#111;color:#fff;padding:12px 18px;border-radius:8px;text-decoration:none">Pay order</a></p>`:"<p>The payment link is not configured yet. Your order is safely reserved as Pending.</p>";const html=`<div style="font-family:Arial,sans-serif;line-height:1.55;color:#111"><h2>We received your Bruis order</h2><p>Hi ${htmlEscape(fullName)},</p><p>Your order <strong>${htmlEscape(reference)}</strong> is saved as <strong>Pending</strong>. We will only send it to production after the transfer has been manually verified.</p><p><strong>Total: ${money(totalCents)}</strong><br>Products: ${money(subtotalCents)}<br>Shipping: ${money(shippingCents)}<br>Payment reference: <strong>${htmlEscape(reference)}</strong></p>${payLine}<p>If you use bunq.me, enter exactly <strong>${money(totalCents)}</strong> and use <strong>${htmlEscape(reference)}</strong> as the description/reference.</p><p>You will receive another email as soon as your shipment is on the way.</p></div>`;const plain=`We received your Bruis order ${reference}.\nStatus: Pending\nTotal: ${money(totalCents)}\nShipping: ${money(shippingCents)}\nPayment reference: ${reference}\n${payment.url?`Payment link: ${payment.url}\n`:""}We only send the order to production after the transfer is manually verified. You will receive another email when the shipment is on the way.`;const mailed=await sendEmail(email,`Bruis order ${reference} received`,html,plain);if(mailed.ok)await supabase.from("shop_orders").update({order_confirmation_notified_at:new Date().toISOString()}).eq("id",orderId);else if(!mailed.skipped)await supabase.from("shop_orders").update({last_error:mailed.error}).eq("id",orderId);return json(req,{ok:true,order_id:orderId,status:"pending",subtotal_cents:subtotalCents,shipping_cents:shippingCents,total_cents:totalCents,shipping_method:shippingMethod,payment_reference:reference,payment_provider:payment.provider,payment_url:payment.url,payment_expires_at:payment.expires_at,confirmation_token:confirmationToken,confirmation_email_sent:!!mailed.ok});}catch(error){console.error("shop-manual-checkout-v825 failed",error);return json(req,{error:"checkout_failed",detail:text(error instanceof Error?error.message:error).slice(0,400)},502);}});
