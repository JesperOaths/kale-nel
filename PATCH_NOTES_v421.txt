GEJAST v421 emergency recovery

What changed:
- disables the global auto-run strict logged-out redirect from gejast-config.js
- keeps the shared version sync and MutationObserver
- intended to restore site loading after the v420 sitewide sweep started forcing every page through the shared config boot path

Likely cause of the outage:
- v420 pointed every page to shared gejast-config.js
- the strict redirect guard inside gejast-config.js then auto-ran everywhere
- that global boot-time redirect was too aggressive for the live site state
