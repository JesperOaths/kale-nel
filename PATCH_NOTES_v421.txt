GEJAST paardenrace patch v421

GitHub zip files:
- paardenrace.html
- gejast-config.js
- PATCH_NOTES_v421.txt

Separate SQL:
- gejast_v421_paardenrace.sql

Changes:
- frontend now polls automatically every ~1.2s for lobby/open-room updates
- if a room disappears, the page clears the stale lobby view automatically
- create room now always uses a fresh random room code
- stale/null room-player rows are normalized or deleted server-side
- create/join/leave room functions were rebuilt to stop the 'Room niet gevonden' create bug
