GEJAST patch v412

Files included
- login.html
- gejast-config.js

What changed
- Fixed login.html boot crash caused by missing forgotPinModal DOM binding.
- Added a defensive null guard around the forgot-pin modal handlers so the page no longer aborts if the modal node is absent.
- Bumped visible shared config version from v411 to v412.
- Bumped login.html to load gejast-config.js?v635 so the fixed page pulls the new shared config version cleanly.

SQL
- None required.

Why this fixes the freeze
- The previous login page referenced forgotPinModal before defining it, which throws a ReferenceError and aborts the rest of the login boot script.
- That stops the later initialization path from finishing and makes the page appear broken/frozen.
