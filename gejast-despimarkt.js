(function(){
  if (window.GEJAST_DESPIMARKT) return;
  const cfg = window.GEJAST_CONFIG || {};
  const VERSION = 'v696';
  const RPC = {
    dashboard:'get_despimarkt_dashboard_v669', market:'get_despimarkt_market_v669', create:'create_despimarkt_market_v669', buy:'buy_despimarkt_position_v669',
    wallet:'get_despimarkt_wallet_v669', ladder:'get_despimarkt_ladder_v669', stats:'get_despimarkt_stats_v669',
    debts:'get_despimarkt_debts_v669', createDebt:'create_despimarkt_debt_v669', settleDebt:'settle_despimarkt_debt_v669',
    adminAudit:'admin_get_despimarkt_runtime_audit_v669', adminResolve:'admin_resolve_despimarkt_market_v669', adminRefund:'admin_refund_despimarkt_market_v669', adminDelete:'admin_delete_despimarkt_market_v669', adminAdjust:'admin_adjust_despimarkt_wallet_v669'
  };
  function qs(id){return document.getElementById(id)}
  function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));}
  function money(v){const n=Number(v||0);return `C ${Math.round(n)}`;}
  function visibleWalletBalance(data){const w=Number(data?.wallet?.balance ?? 100);const ledger=Array.isArray(data?.ledger)?data.ledger:[];return (!ledger.length && w > 100) ? 100 : w;}
  function marketEmptyHtml(){return '<div class="empty-state"><b>Geen open markets gevonden.</b><br>Maak een market of open de marketlijst zodra de SQL-runtime markets teruggeeft.</div><div class="row"><a class="btn gold" href="./despimarkt_create.html">Nieuwe market</a><a class="btn alt" href="./despimarkt_market.html">Marketlijst</a></div>';}
  function scope(value){const raw=String(value||'').toLowerCase();if(raw==='family')return'family';try{const q=new URLSearchParams(location.search);if(q.get('scope')==='family')return'family';}catch(_){}return'friends';}
  function token(){try{if(cfg.getPlayerSessionToken)return String(cfg.getPlayerSessionToken()||'').trim();}catch(_){}return localStorage.getItem('jas_session_token_v11')||localStorage.getItem('jas_session_token_v10')||sessionStorage.getItem('jas_session_token_v11')||sessionStorage.getItem('jas_session_token_v10')||'';}
  function adminToken(){try{if(window.GEJAST_ADMIN_SESSION&&window.GEJAST_ADMIN_SESSION.getToken)return String(window.GEJAST_ADMIN_SESSION.getToken()||'').trim();}catch(_){}return localStorage.getItem('jas_admin_session_v8')||sessionStorage.getItem('jas_admin_session_v8')||'';}
  function headers(){return {'Content-Type':'application/json',apikey:cfg.SUPABASE_PUBLISHABLE_KEY||'',Authorization:`Bearer ${cfg.SUPABASE_PUBLISHABLE_KEY||''}`,Accept:'application/json'};}
  async function parse(res){const txt=await res.text();let data=null;try{data=txt?JSON.parse(txt):null;}catch(_){throw new Error(txt||`HTTP ${res.status}`);}if(!res.ok)throw new Error(data?.message||data?.error||data?.details||data?.hint||`HTTP ${res.status}`);return data;}
  async function rpc(name,payload){if(!cfg.SUPABASE_URL||!cfg.SUPABASE_PUBLISHABLE_KEY)throw new Error('Supabase config ontbreekt.');const raw=await fetch(`${cfg.SUPABASE_URL}/rest/v1/rpc/${name}`,{method:'POST',mode:'cors',cache:'no-store',headers:headers(),body:JSON.stringify(payload||{})}).then(parse);return raw&&raw[name]!==undefined?raw[name]:raw;}
  function marketHref(id){return `./despimarkt_market.html?market_id=${encodeURIComponent(id||'')}${scope()==='family'?'&scope=family':''}`;}
  function pageScope(){return scope();}
  async function dashboard(opts={}){return rpc(RPC.dashboard,{session_token_input:token()||null,site_scope_input:scope(opts.scope),limit_input:Number(opts.limit||25)});}
  async function market(id,opts={}){return rpc(RPC.market,{market_id_input:id,session_token_input:token()||null,site_scope_input:scope(opts.scope)});}
  async function createMarket(input){return rpc(RPC.create,{session_token_input:token(),site_scope_input:scope(input.scope),title_input:input.title,description_input:input.description||'',outcome_a_input:input.outcomeA||'Ja',outcome_b_input:input.outcomeB||'Nee',close_at_input:input.closeAt||null});}
  async function buyPosition(input){return rpc(RPC.buy,{session_token_input:token(),site_scope_input:scope(input.scope),market_id_input:input.marketId,outcome_input:input.outcome,stake_input:Number(input.stake||0)});}
  async function wallet(opts={}){return rpc(RPC.wallet,{session_token_input:token(),site_scope_input:scope(opts.scope),limit_input:Number(opts.limit||50)});}
  async function ladder(opts={}){return rpc(RPC.ladder,{site_scope_input:scope(opts.scope),limit_input:Number(opts.limit||50)});}
  async function stats(opts={}){return rpc(RPC.stats,{site_scope_input:scope(opts.scope)});}
  async function debts(opts={}){return rpc(RPC.debts,{session_token_input:token()||null,site_scope_input:scope(opts.scope),limit_input:Number(opts.limit||50)});}
  async function createDebt(input){return rpc(RPC.createDebt,{session_token_input:token(),site_scope_input:scope(input.scope),target_player_name_input:input.targetPlayer,amount_input:Number(input.amount||0),reason_input:input.reason||'',due_at_input:input.dueAt||null});}
  async function settleDebt(id){return rpc(RPC.settleDebt,{session_token_input:token(),debt_id_input:id});}
  async function adminAudit(opts={}){return rpc(RPC.adminAudit,{admin_session_token_input:adminToken(),site_scope_input:scope(opts.scope),limit_input:Number(opts.limit||50)});}
  async function adminResolve(input){return rpc(RPC.adminResolve,{admin_session_token_input:adminToken(),market_id_input:input.marketId,winning_outcome_input:input.outcome,reason_input:input.reason||'admin resolve'});}
  async function adminRefund(input){return rpc(RPC.adminRefund,{admin_session_token_input:adminToken(),market_id_input:input.marketId,reason_input:input.reason||'admin refund'});}
  async function adminDelete(input){return rpc(RPC.adminDelete,{admin_session_token_input:adminToken(),market_id_input:input.marketId,reason_input:input.reason||'admin delete'});}
  async function adminAdjust(input){return rpc(RPC.adminAdjust,{admin_session_token_input:adminToken(),site_scope_input:scope(input.scope),target_player_name_input:input.playerName,delta_input:Number(input.delta||0),reason_input:input.reason||'admin adjustment'});}
  function marketCard(m,opts={}){const status=String(m.status||'open');const id=m.id||m.market_id||'';const admin=opts.admin&&id?`<div class="row" style="margin-top:10px"><button class="btn gold" data-admin-resolve="A" data-market="${esc(id)}">Resolve A</button><button class="btn" data-admin-resolve="B" data-market="${esc(id)}">Resolve B</button><button class="btn alt" data-admin-refund data-market="${esc(id)}">Refund</button><button class="btn bad" data-admin-delete data-market="${esc(id)}">Delete</button></div>`:'';return `<article class="market-card" data-market-id="${esc(id)}"><div class="split"><div><div class="market-title">${esc(m.title||m.question||'Market')}</div><div class="meta">${esc(m.description||m.details||'')}</div></div><span class="pill ${status==='open'?'ok':status==='resolved'?'warn':'bad'}">${esc(status)}</span></div><div class="row"><span class="pill">${esc(m.outcome_a||'A')}: ${money(m.pool_a)}</span><span class="pill">${esc(m.outcome_b||'B')}: ${money(m.pool_b)}</span><span class="pill">totaal ${money(m.total_pool||Number(m.pool_a||0)+Number(m.pool_b||0))}</span></div><a class="btn alt" href="${marketHref(id)}">Open market</a>${admin}</article>`;}
  function renderMarkets(rows,target,opts={}){const el=typeof target==='string'?qs(target):target;if(!el)return;el.innerHTML=(rows&&rows.length)?rows.map(m=>marketCard(m,opts)).join(''):marketEmptyHtml();}
  function renderPositions(rows,target){const el=typeof target==='string'?qs(target):target;if(!el)return;el.innerHTML=(rows&&rows.length)?rows.map(p=>`<div class="ledger-row split"><div><b>${esc(p.market_title||p.title||'Market')}</b><div class="meta">${esc(p.player_name||'Speler')} - ${esc(p.outcome||p.side||'positie')} - ${esc(p.status||'open')}</div></div><b>${money(p.stake||p.amount||0)}</b></div>`).join(''):'<div class="empty-state">Nog geen actieve posities/bets gevonden.</div>';}
  function renderWallet(data,target){const el=typeof target==='string'?qs(target):target;if(!el)return;const w=data.wallet||{};const ledger=Array.isArray(data.ledger)?data.ledger:[];el.innerHTML=`<div class="panel tinted-gold"><div class="eyebrow">Saldo</div><div class="balance">${money(visibleWalletBalance(data))}</div><div class="meta">${esc(w.player_name||data.player_name||'')}</div></div><div class="rowlist">${ledger.map(r=>`<div class="ledger-row split"><div><b>${esc(r.reason||'ledger')}</b><div class="meta">${esc(r.created_at||'')}</div></div><b>${money(r.delta)}</b></div>`).join('')||'<div class="empty-state">Nog geen ledger.</div>'}</div>`;}
  async function loadHubPage(){const status=qs('status');try{if(status)status.textContent='Beurs laden...';const data=await dashboard({limit:20});if(qs('heroCoins'))qs('heroCoins').textContent=money(visibleWalletBalance(data));if(qs('heroLeader'))qs('heroLeader').textContent=data.leaderboard?.[0]?.player_name||'--';if(qs('heroMarkets'))qs('heroMarkets').textContent=String(data.totals?.open_markets||((data.markets||[]).filter(m=>String(m.status||'open')==='open').length));renderMarkets(data.markets||[],qs('marketList')||qs('marketsBox')||qs('openMarketsBox'));renderPositions(data.positions||data.bets||[],qs('positionsBox')||qs('betsBox'));if(qs('walletBox'))renderWallet(data,qs('walletBox'));if(status)status.textContent='Beurs geladen.';}catch(e){if(status)status.textContent=e.message||String(e);}}
  async function loadCreatePage(){const form=qs('marketForm');const status=qs('status');if(!form)return;form.addEventListener('submit',async(ev)=>{ev.preventDefault();try{if(status)status.textContent='Market maken...';const out=await createMarket({title:qs('titleInput').value.trim(),description:qs('descriptionInput').value.trim(),outcomeA:qs('outcomeAInput').value.trim(),outcomeB:qs('outcomeBInput').value.trim(),closeAt:qs('closeAtInput').value||null});if(status)status.textContent='Market gemaakt.';location.href=marketHref(out.market_id||out.id);}catch(e){if(status)status.textContent=e.message||String(e);}});}
  async function loadMarketPage(){const id=new URLSearchParams(location.search).get('market_id')||new URLSearchParams(location.search).get('id');const box=qs('marketBox'),status=qs('status');if(!id){const data=await dashboard({limit:20});renderMarkets(data.markets||[],box);return;}async function refresh(){const data=await market(id);const m=data.market||{};const pos=data.positions||[];box.innerHTML=`<section class="panel"><div class="split"><div><h1>${esc(m.title)}</h1><p>${esc(m.description||'')}</p></div><span class="pill ${m.status==='open'?'ok':'warn'}">${esc(m.status)}</span></div><div class="grid two"><div class="panel tinted-gold"><h2>${esc(m.outcome_a)}</h2><div class="balance">${money(m.pool_a||0)}</div><button class="btn gold" data-buy="A">Koop A</button></div><div class="panel tinted-sky"><h2>${esc(m.outcome_b)}</h2><div class="balance">${money(m.pool_b||0)}</div><button class="btn" data-buy="B">Koop B</button></div></div><div class="field"><label>Inzet</label><input id="stakeInput" type="number" min="1" step="1" value="25"></div></section><section class="panel"><h2>Posities</h2>${pos.map(p=>`<div class="ledger-row split"><span>${esc(p.player_name)} · ${esc(p.outcome)} · ${esc(p.status)}</span><b>${money(p.stake)}</b></div>`).join('')||'<div class="empty-state">Nog geen posities.</div>'}</section>`;box.querySelectorAll('[data-buy]').forEach(btn=>btn.onclick=async()=>{try{if(status)status.textContent='Kopen...';await buyPosition({marketId:id,outcome:btn.dataset.buy,stake:Number(qs('stakeInput').value||0)});if(status)status.textContent='Positie gekocht.';await refresh();}catch(e){if(status)status.textContent=e.message||String(e);}});}try{await refresh();}catch(e){if(status)status.textContent=e.message||String(e);}}
  async function loadWalletPage(){const status=qs('status');try{const data=await wallet({limit:80});renderWallet(data,qs('walletBox'));if(status)status.textContent='Wallet geladen.';}catch(e){if(status)status.textContent=e.message||String(e);}}
  async function loadDebtsPage(){const status=qs('status');async function refresh(){const data=await debts({limit:80});const box=qs('debtBox');box.innerHTML=(data.debts||[]).map(d=>`<div class="debt-row split"><div><b>${esc(d.debtor_name)} → ${esc(d.creditor_name)}</b><div class="meta">${esc(d.reason||'')} · ${esc(d.status)}</div></div><div><b>${money(d.amount)}</b> ${d.status==='open'?`<button class="btn alt" data-settle="${d.id}">Betaal</button>`:''}</div></div>`).join('')||'<div class="empty-state">Geen schulden.</div>';box.querySelectorAll('[data-settle]').forEach(btn=>btn.onclick=async()=>{try{await settleDebt(btn.dataset.settle);await refresh();}catch(e){if(status)status.textContent=e.message||String(e);}});}const form=qs('debtForm');if(form)form.onsubmit=async(ev)=>{ev.preventDefault();try{await createDebt({targetPlayer:qs('targetInput').value,amount:qs('amountInput').value,reason:qs('reasonInput').value,dueAt:qs('dueAtInput').value||null});await refresh();}catch(e){if(status)status.textContent=e.message||String(e);}};await refresh();}
  async function loadLadderPage(){const data=await ladder({limit:100});const box=qs('ladderBox');box.innerHTML=(data.leaderboard||[]).map((r,i)=>`<div class="leader-row split"><div><b>#${i+1} ${esc(r.player_name)}</b><div class="meta">markets ${r.positions||0} · open ${money(r.open_stake||0)}</div></div><b>${money(r.balance)}</b></div>`).join('')||'<div class="empty-state">Geen ladder.</div>';}
  async function loadStatsPage(){const data=await stats({});const box=qs('statsBox');box.innerHTML=`<div class="grid three">${Object.entries(data.totals||{}).map(([k,v])=>`<div class="panel"><div class="eyebrow">${esc(k)}</div><div class="balance">${esc(v)}</div></div>`).join('')}</div>`;}
  async function loadAdminPage(){const status=qs('adminStatus')||qs('status');async function refresh(){const data=await adminAudit({limit:100});if(qs('adminRaw'))qs('adminRaw').textContent=JSON.stringify(data,null,2);renderMarkets(data.markets||[],qs('adminMarkets'),{admin:true});if(qs('walletAdminList'))qs('walletAdminList').innerHTML=(data.wallets||[]).map(w=>`<div class="wallet-row split"><b>${esc(w.player_name)}</b><span>${money(w.balance)}</span></div>`).join('');if(status)status.textContent='Admin geladen.';}document.addEventListener('click',async(ev)=>{const a=ev.target.closest('[data-admin-resolve],[data-admin-refund],[data-admin-delete]');if(!a)return;try{const marketId=a.getAttribute('data-market');if(a.dataset.adminResolve)await adminResolve({marketId,outcome:a.dataset.adminResolve});else if(a.dataset.adminRefund!==undefined)await adminRefund({marketId});else await adminDelete({marketId});await refresh();}catch(e){if(status)status.textContent=e.message||String(e);}});const form=qs('adjustForm');if(form)form.onsubmit=async(ev)=>{ev.preventDefault();try{await adminAdjust({playerName:qs('adjustPlayerName').value,delta:qs('adjustDelta').value,reason:qs('adjustReason').value});await refresh();}catch(e){if(status)status.textContent=e.message||String(e);}};await refresh();}
  window.GEJAST_DESPIMARKT={VERSION,RPC,scope,token,adminToken,rpc,dashboard,market,createMarket,buyPosition,wallet,ladder,stats,debts,createDebt,settleDebt,adminAudit,adminResolve,adminRefund,adminDelete,adminAdjust,renderMarkets,renderPositions,renderWallet,loadHubPage,loadCreatePage,loadMarketPage,loadWalletPage,loadDebtsPage,loadLadderPage,loadStatsPage,loadAdminPage,marketHref,money};
})();
