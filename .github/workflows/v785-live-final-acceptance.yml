name: v785 live final acceptance and freeze

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  final-acceptance:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Permanent repository verification before live promotion
        run: npm run verify
      - name: Hardened live v785 route suite
        env:
          GEJAST_EXPECTED_VERSION: v785
          GEJAST_SMOKE_RETRIES: 5
        run: npm run smoke:live
      - name: Live read-only beta surfaces
        run: |
          npm run smoke:beta:read
          npm run smoke:beta:extended
      - name: Live performance budgets
        run: npm run smoke:beta:perf
      - name: Install temporary browser/accessibility tooling
        run: |
          npm install --no-save playwright axe-core
          npx playwright install --with-deps chromium
      - name: Full 70-combination live Chromium acceptance
        run: node scripts/v785-live-final-acceptance.mjs
      - name: Promote v785 and create authoritative finalized baseline
        run: node scripts/promote-v785-finalized-baseline.mjs
      - name: Remove temporary final-acceptance machinery
        run: |
          rm -f V785_LIVE_FINAL_ACCEPTANCE.json
          rm -f scripts/v785-live-final-acceptance.mjs
          rm -f scripts/promote-v785-finalized-baseline.mjs
          rm -f .github/workflows/v785-live-final-acceptance.yml
      - name: Verify clean promoted baseline
        run: npm run verify
      - name: Commit durable live/freeze evidence only
        run: |
          rm -rf node_modules
          git config core.whitespace cr-at-eol
          git diff --check
          test ! -e V785_LIVE_FINAL_ACCEPTANCE.json
          test ! -e scripts/v785-live-final-acceptance.mjs
          test ! -e scripts/promote-v785-finalized-baseline.mjs
          test ! -e .github/workflows/v785-live-final-acceptance.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "docs: freeze finalized live v785 baseline"
          git push origin HEAD:${{ github.head_ref }}
