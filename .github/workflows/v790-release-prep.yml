name: v790 release prep

on:
  push:
    branches:
      - 'agent/v790-pikken-pik-conversion'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  prep:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
        with:
          ref: agent/v790-pikken-pik-conversion
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - name: Apply narrow repair and synchronize release identity
        run: node scripts/prepare-v790-pikken.mjs
      - name: Verify candidate before committing generated sync
        run: npm run verify
      - name: Show generated release scope
        run: git status --short
      - name: Commit generated candidate changes if needed
        run: |
          if git diff --quiet && git diff --cached --quiet; then
            echo 'No generated changes to commit.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: prepare v790 Pikken pik conversion'
          git push origin HEAD:agent/v790-pikken-pik-conversion
