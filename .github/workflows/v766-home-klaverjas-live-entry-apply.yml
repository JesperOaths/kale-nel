name: v766 homepage Klaverjas live-entry apply

on:
  pull_request:
    branches:
      - main
    types: [opened]

permissions:
  contents: write

jobs:
  apply:
    if: github.event.pull_request.head.ref == 'agent/v766-home-klaverjas-live-entry'
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: agent/v766-home-klaverjas-live-entry
          fetch-depth: 0
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - name: Install pinned dependencies
        run: npm ci
      - name: Apply narrow homepage patch
        run: node scripts/apply-v766-home-klaverjas-live-entry.mjs
      - name: Synchronize frontend version markers
        run: node fix-version-drift.mjs
      - name: Verify generated state
        run: npm run verify
      - name: Commit generated frontend release safely
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -u
          git add check-home-klaverjas-live-entry-v766.mjs
          if git diff --cached --quiet; then
            echo "No generated changes to commit."
            exit 0
          fi
          if git diff --cached --name-only | grep -q '^node_modules/'; then
            echo "Refusing to commit node_modules"
            exit 1
          fi
          git commit -m "v766 add homepage Klaverjas live entry"
          git push origin HEAD:agent/v766-home-klaverjas-live-entry
