name: v772 postmerge public edge proof

on:
  pull_request:
    paths:
      - 'check-live-v772-finalization.mjs'
      - '.github/workflows/v772-postmerge-live-proof.yml'
      - 'beta-readiness.json'

permissions:
  contents: read

jobs:
  live-proof:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out repository
        uses: actions/checkout@v5
      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
      - name: Prove full hardened live route suite at v772
        env:
          GEJAST_EXPECTED_VERSION: v772
          GEJAST_DEPLOY_WAIT_SECONDS: '120'
          GEJAST_SMOKE_RETRIES: '3'
        run: node check-live-routes.mjs
      - name: Prove v772 finalization on public edge
        env:
          GEJAST_DEPLOY_WAIT_SECONDS: '120'
        run: node check-live-v772-finalization.mjs
