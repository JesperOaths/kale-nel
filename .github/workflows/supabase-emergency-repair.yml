name: Supabase emergency repair

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/supabase-emergency-repair.yml"
      - "ops/supabase-emergency-trigger.txt"

permissions:
  contents: read

jobs:
  repair:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
      PGCONNECT_TIMEOUT: "12"
    steps:
      - name: Check out repository
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
      - name: Build masked pooler URLs
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import os, urllib.parse
          u=urllib.parse.urlsplit(os.environ['SUPABASE_DB_URL'])
          pw=urllib.parse.quote(urllib.parse.unquote(u.password or ''), safe='')
          user='postgres.uiqntazgnrxwliaidkmy'
          host='aws-0-eu-west-1.pooler.supabase.com'
          for name,port in [('POOLER_TX_URL',6543),('POOLER_SESSION_URL',5432)]:
              url=f'postgresql://{user}:{pw}@{host}:{port}/postgres?sslmode=require'
              print(f'::add-mask::{url}')
              with open(os.environ['GITHUB_ENV'],'a') as f:
                  f.write(f'{name}={url}\n')
          PY
      - name: Ensure psql
        shell: bash
        run: |
          set -euo pipefail
          command -v psql >/dev/null || { sudo apt-get update -qq && sudo apt-get install -y -qq postgresql-client >/dev/null; }
      - name: Apply bounded rescue
        shell: bash
        run: |
          set -euo pipefail
          targets=("$POOLER_TX_URL" "$POOLER_SESSION_URL" "$SUPABASE_DB_URL")
          labels=("transaction-pooler" "session-pooler" "direct")
          for i in 0 1 2; do
            for attempt in 1 2; do
              echo "attempt ${attempt} via ${labels[$i]}"
              if timeout 25s psql "${targets[$i]}" -X --set ON_ERROR_STOP=1 --file GEJAST_v799z_supabase_connection_rescue.sql; then
                echo "RESCUE_APPLIED=${labels[$i]}"
                exit 0
              fi
              sleep 5
            done
          done
          exit 1
