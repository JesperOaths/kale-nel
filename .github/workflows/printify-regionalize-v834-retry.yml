name: Inspect Despinoza Gildan 5000 text layers

"on":
  push:
    branches:
      - main
    paths:
      - '.github/workflows/printify-regionalize-v834-retry.yml'

permissions:
  contents: read

jobs:
  regionalize:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Inspect source and regional text layers
        shell: bash
        env:
          ENDPOINT: https://uiqntazgnrxwliaidkmy.supabase.co/functions/v1/printify-despinoza-diagnostic-v834
          RUN_KEY: despinoza-diag-20260916-J7qvF4bM
        run: |
          set -euo pipefail
          curl --silent --show-error --max-time 900 \
            --request POST "$ENDPOINT" \
            --header 'content-type: application/json' \
            --header "x-run-key: $RUN_KEY" \
            --data '{}' | jq .
