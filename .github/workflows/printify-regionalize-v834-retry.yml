name: Diagnose Despinoza Gildan 5000 sources

"on":
  push:
    branches:
      - main
    paths:
      - '.github/workflows/printify-regionalize-v834-retry.yml'

permissions:
  contents: read

jobs:
  regionalize:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Discover source products
        shell: bash
        env:
          ENDPOINT: https://uiqntazgnrxwliaidkmy.supabase.co/functions/v1/printify-despinoza-gildan5000-v834
          RUN_KEY: despinoza-g5000-20260916-Yp6Wq9uN
        run: |
          set -euo pipefail
          curl --silent --show-error --max-time 900 \
            --request POST "$ENDPOINT" \
            --header 'content-type: application/json' \
            --header "x-run-key: $RUN_KEY" \
            --data '{}' | jq .
