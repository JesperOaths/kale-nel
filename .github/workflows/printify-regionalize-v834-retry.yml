name: Inspect Despinoza regional mockups

"on":
  push:
    branches:
      - main
    paths:
      - '.github/workflows/printify-regionalize-v834-retry.yml'

permissions:
  contents: read

jobs:
  regionalize:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Inspect source and hidden regional collar mockups
        shell: bash
        env:
          ENDPOINT: https://uiqntazgnrxwliaidkmy.supabase.co/functions/v1/printify-despinoza-target-images-v834
          RUN_KEY: despinoza-target-images-20260916-R8mQ2
        run: |
          set -euo pipefail
          curl --silent --show-error --max-time 900 \
            --request POST "$ENDPOINT" \
            --header 'content-type: application/json' \
            --header "x-run-key: $RUN_KEY" \
            --data '{}' | jq .
