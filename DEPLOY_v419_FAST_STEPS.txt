GEJAST v419 fast steps

1. Upload frontend files:
   - gejast-config.js
   - paardenrace.html
   - gejast-paardenrace.js

2. Run in Supabase SQL editor:
   - gejast_v419_paardenrace_playerid_cleanup_roomname.sql

3. Hard refresh paardenrace.html.

4. Confirm:
   - custom lobbynaam works on create
   - join list can be filtered by lobbynaam
   - stale 0/1-player lobbies disappear after ~2 minutes
   - join no longer fails on null player_id
