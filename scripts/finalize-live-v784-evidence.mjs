#!/usr/bin/env node
import fs from 'node:fs';

const releaseMerge='fa16723ac12e341ba09b810f82dcc86b9c4414f6';
const version=fs.readFileSync('VERSION','utf8').trim();
if(version!=='v784') throw new Error(`expected v784, got ${version}`);
const readiness=JSON.parse(fs.readFileSync('beta-readiness.json','utf8'));
if(readiness.deployment_identity?.release_candidate_version!=='v784') throw new Error('expected v784 release-candidate marker');
readiness.site_version='live v784 / current frontend release v784';
readiness.last_updated='2026-08-10';
readiness.deployment_identity.live_version='v784';
readiness.deployment_identity.frontend_release_merge=releaseMerge;
readiness.deployment_identity.repository_head_at_audit=releaseMerge;
delete readiness.deployment_identity.release_candidate_version;
readiness.deployment_identity.note='2026-08-10 v784 post-merge public-edge proof PASS: public VERSION v784 and hardened 35-route live suite PASS. The v784 no-write usability audit covered 29 important user-facing routes at phone and desktop size (58 combinations); scorer Later, Toepen Annuleren, Drinks Back handlers and Beurs feedback were classified as intentional working controls, while invalid activation state was the one confirmed dead end. Live activate.html and gejast-account-runtime.js now ship PIN/submit controls disabled, stop missing-token flow before activation-context lookup, keep invalid/expired context non-actionable with Niet beschikbaar identity text, expose Terug naar inloggen, and only re-enable after valid context. Isolated no-write live Chromium confirmed a missing token makes zero activation-context calls and zero activation-write RPCs, while a locally simulated expired token makes only its context lookup and still makes zero activation-write RPCs; both states keep the form disabled and show the login fallback. The only unrelated blocked POST was the read-only get_scope_hardening_bundle_v672 scope lookup, intercepted locally. The prior no-write runtime-stability Chromium audit remains preserved across all 35 public routes at phone and desktop size (70 combinations): zero JavaScript page crashes, zero console-error pages, zero same-origin HTTP 4xx/5xx resources, zero stuck/hidden pages and zero empty pages; 446 non-GET requests were intercepted locally, and live v783 redirect aliases preserve zero aborted same-origin resources before navigation. The prior isolated no-write live mobile Chromium proof remains preserved: no Drinks stats-queue ReferenceError, the Drinks speed selector remains >=44px high, hidden verification-float controls are inert/non-focusable and cannot receive focus, and both Beerpong navigation links plus Pussycup checkboxes meet the >=24px target baseline; all non-GET browser traffic was intercepted locally. The 70/70 naming closure and v779 keyboard baseline remain preserved, the v780 live Chromium/axe proof remains preserved with zero axe violations on the nine repaired pages, and the v782 no-write responsive Chromium proof remains preserved. Infrastructure-only v775b public-header code remains merged but not live pending authenticated Cloudflare deployment.';
const staticCheck=(readiness.baseline_checks||[]).find(x=>x.id==='static_integrity');
if(staticCheck) staticCheck.evidence='Current Node 24 verification passes all existing frontend/backend/security/accessibility regressions through v784. The accessibility naming backlog remains closed 70/70: 58 static controls are protected by v777 and 12 runtime-generated controls by v778; v779 keyboard/focus remains protected; v780 rendered Chromium/axe proof remains preserved with zero axe violations on the nine repaired pages. v781 protects the Drinks stats-queue repair, mobile-sized Drinks/Beerpong targets and the aria-hidden/inert verification-float lifecycle. v782 protects the Drinks bar renderer plus responsive homepage/Klaverjas layout owners and intentional Boerenbridge scrolling. v783 protects runtime-light direct score/Pikken spectator redirect aliases. v784 protects disabled-by-default activation controls, missing/invalid-context early exits, clear login fallback and valid-context-only submit wiring.';
const liveRoutes=(readiness.baseline_checks||[]).find(x=>x.id==='live_routes');
if(liveRoutes) liveRoutes.evidence='2026-08-10 live v784 hardened 35-route suite PASS. Isolated no-write live Chromium confirms missing-token activation makes zero activation-context calls and zero activation-write RPCs, while a locally simulated expired context keeps activation disabled and makes zero activation-write RPCs; both unusable-link states show a clear login fallback. The prior 70-combination no-write runtime-stability audit reported zero page crashes, console-error pages, same-origin 4xx/5xx resources, stuck/hidden pages or empty pages. The prior isolated no-write mobile Chromium proof remains preserved: no Drinks stats-queue ReferenceError, >=44px Drinks speed selector, hidden verification float inert/non-focusable, and >=24px scoped Beerpong navigation/Pussycup targets. The v782 no-write responsive Chromium evidence also remains preserved.';
if((readiness.beta_gaps||[]).filter(x=>x.status==='verified_complete').length!==12) throw new Error('readiness must remain 12/12');
if((readiness.beta_gaps||[]).some(x=>x.status==='needs_permission'||x.status==='blocked_external')) throw new Error('unexpected readiness permission/blocked gap');
fs.writeFileSync('beta-readiness.json',JSON.stringify(readiness,null,2)+'\n');
const checklist=JSON.parse(fs.readFileSync('beta-live-write-checklist.json','utf8'));
if(checklist.site_version!=='v784'||!Array.isArray(checklist.items)||checklist.items.length!==0) throw new Error('v784 live-write checklist must remain empty');
console.log('v784 live evidence prepared: activation dead-end proof live-clean, readiness 12/12, zero armed writes.');
