#!/usr/bin/env node
import fs from 'node:fs';
import { execFileSync } from 'node:child_process';

const releaseMerge='bbfc7c4afe232c0e9046d333e2b49b93b9159a2d';
const readiness=JSON.parse(fs.readFileSync('beta-readiness.json','utf8'));
readiness.last_updated='2026-08-11';
readiness.site_version='live v787 / current frontend release v787';
readiness.deployment_identity=readiness.deployment_identity||{};
readiness.deployment_identity.status='verified_complete';
readiness.deployment_identity.live_version='v787';
readiness.deployment_identity.frontend_release_merge=releaseMerge;
readiness.deployment_identity.repository_head_at_audit=releaseMerge;
delete readiness.deployment_identity.release_candidate_version;
readiness.deployment_identity.note='2026-08-11 live v787 production closure PASS after merge '+releaseMerge+': public VERSION v787, hardened 35-route suite, base/extended read-only beta checks and configured performance budgets passed. Firefox + WebKit on phone + desktop proved all four Family redirect aliases reach the exact scope=family canonical first destination with zero wrong /familie/ subresource requests. Beta readiness remains 12/12 and no production write target is armed.';
const evidence=Array.isArray(readiness.deployment_identity.evidence)?readiness.deployment_identity.evidence:[];
const liveEvidence='2026-08-11 live v787: Firefox+WebKit Family alias proof 16/16 PASS; zero wrong /familie/ subresource requests; 35-route/read-only/performance closure PASS';
if(!evidence.includes(liveEvidence)) evidence.push(liveEvidence);
readiness.deployment_identity.evidence=evidence;
const baseline=Array.isArray(readiness.baseline_checks)?readiness.baseline_checks:[];
const staticCheck=baseline.find(x=>x.id==='static_integrity');
if(staticCheck&&!String(staticCheck.evidence).includes('v787')) staticCheck.evidence += ' v787 protects the four Family compatibility wrappers as <=1800-byte redirect-only aliases with no external runtime/network bootstrap and exact scope=family fallbacks.';
const liveRoutes=baseline.find(x=>x.id==='live_routes');
if(liveRoutes) liveRoutes.evidence='2026-08-11 live v787 hardened 35-route suite PASS; Firefox + WebKit Family alias proof PASS across 16 phone/desktop combinations with exact family-scope first destinations and zero wrong /familie/ subresource requests. '+String(liveRoutes.evidence||'');
const perf=baseline.find(x=>x.id==='performance_beta_pass');
if(perf) perf.evidence='2026-08-11 live v787 configured performance budget probe PASS. '+String(perf.evidence||'');
fs.writeFileSync('beta-readiness.json',JSON.stringify(readiness,null,2)+'\n');

const finalized=`# Kalenel — finalized project state\n\nFinalized on **2026-08-11** after the v787 cross-engine production-acceptance closure.\n\n## Authoritative stable baseline\n\n- Finalized frontend baseline: **v787**\n- Current live frontend at freeze: **v787**\n- Frontend release merge: \`${releaseMerge}\`\n- Public frontend: GitHub Pages/static deployment from \`main\`\n- Protected admin perimeter: Cloudflare Worker build **v762** on \`admin.kalenel.nl\` (separate version stream from the frontend)\n- Backend/data: Supabase authenticated RPC contracts\n- Push dispatcher: Node 24 production runtime\n- Beta readiness: **12/12 verified complete**, 0 permission-gated, 0 blocked\n- Live-write checklist: **0 armed mutation targets**\n\n## Final production acceptance\n\nLive v787 passed the hardened **35-route** public health suite, base and extended read-only beta checks, and configured performance budgets. The v786 full Chromium freeze remains preserved: all 35 authoritative public routes at phone and desktop size (**70 combinations**) completed with zero page exceptions, zero console-error pages, zero same-origin GET/HEAD failures, zero stuck auth/loading states, zero whole-page overflow, zero positive-tabindex/hidden-focus violations, and zero serious/critical axe accessibility violations.\n\nv787 adds a cross-engine closure for the four Family compatibility aliases. Firefox and WebKit each proved \`familie/index.html\`, \`familie/login.html\`, \`familie/scorer.html\`, and \`familie/leaderboard.html\` at phone and desktop size (**16 focused combinations**). Every alias reached its exact canonical \`scope=family\` first destination with **zero wrong \`/familie/\` subresource requests**. The wrappers are redirect-only and no longer bootstrap normal runtime/config code before navigation.\n\nThe prior Rad runtime multi-width proof at **320, 360, 390, 430 and 760px** remains protected. The shared mobile runtime sizes the wheel from its container with \`min(100%,460px)\`, does not reintroduce the faulty \`96vw\` override, and keeps the page and wheel inside the mobile panel.\n\n## User-facing product surface\n\nFriends and Family scopes remain isolated. The public product includes the homepage/game launcher, profiles and account claim/login/activation flows, Klaverjas scorer/live/online/leaderboard surfaces, Toepen, Beerpong, Boerenbridge, Pikken, Paardenrace, Drinks and verification/statistics, Caute Rad, and Beurs d'Espinoza/Caute Coins surfaces. Compatibility/deep-link aliases are retained only where current runtime navigation still depends on them; redirect aliases must remain runtime-light.\n\n## Security and operations\n\nThe public admin entry redirects to the protected admin host. The Cloudflare outer GitHub OAuth allowlist remains separate from the inner Supabase admin username/password/TOTP boundary. Active admin mutation paths remain session/scope guarded. Public operational consoles removed during the v775 cleanup remain absent. No Cloudflare paid service is required by this baseline.\n\nProduction mutation proof is closed rather than continuously re-run: Drinks, Toepen, Klaverjas, Beerpong, Boerenbridge and profile persistence have controlled evidence and permanent guards. Completed write targets stay disarmed. New consequential writes require a newly scoped approval gate instead of reusing historical proof machinery.\n\n## Stable invariants\n\n- Ice remains exactly **2.8 units**.\n- Daily counters/polls use the Amsterdam **06:00 -> 06:00** boundary.\n- Friends/Family scope isolation remains mandatory.\n- Completed live-write targets remain disarmed.\n- Admin Worker build **v762** is not the frontend VERSION and must not be mechanically bumped with frontend releases.\n- Historical module/RPC suffixes identify contracts and are not current frontend-version markers.\n- The Rad shared mobile runtime must remain container-bounded; the removed \`96vw\` wheel override must not return.\n- Family redirect aliases must not load normal runtime/config dependencies before redirecting to their canonical \`scope=family\` destination.\n\n## Change policy after freeze\n\nv787 is the stable finished baseline. Future work should normally be a deliberate feature or a specifically reported defect, not open-ended cleanup for its own sake. Change only the affected owner, preserve working behavior outside that scope, add or update a focused regression, bump the frontend VERSION for a real frontend change, and repeat the relevant live proof plus the standard verification suite. Consequential production mutations remain permissioned and should not be manufactured merely for testing.\n`;
fs.writeFileSync('FINALIZED_PROJECT_STATE.md',finalized);

const baselineGuard=`#!/usr/bin/env node\nimport fs from 'node:fs';\nconst root=fs.readFileSync('VERSION','utf8').trim();\nif(root!=='v787'){console.log('v787 finalized-baseline guard historical skip at '+root);process.exit(0);}\nconst failures=[];\nconst readiness=JSON.parse(fs.readFileSync('beta-readiness.json','utf8'));\nconst checklist=JSON.parse(fs.readFileSync('beta-live-write-checklist.json','utf8'));\nconst doc=fs.readFileSync('FINALIZED_PROJECT_STATE.md','utf8');\nif(readiness.site_version!=='live v787 / current frontend release v787') failures.push('readiness site_version must be live v787');\nif(readiness.deployment_identity?.live_version!=='v787') failures.push('deployment live_version must be v787');\nif(readiness.deployment_identity?.frontend_release_merge!=='${releaseMerge}') failures.push('frontend release merge must be ${releaseMerge}');\nif('release_candidate_version' in (readiness.deployment_identity||{})) failures.push('release_candidate_version must be removed after live promotion');\nif(!Array.isArray(checklist.items)||checklist.items.length!==0) failures.push('live-write checklist must remain empty');\nif(checklist.site_version!=='v787') failures.push('live-write checklist site_version must be v787');\nconst gaps=Array.isArray(readiness.beta_gaps)?readiness.beta_gaps:[];\nif(gaps.length!==12||gaps.some(g=>g.status!=='verified_complete')) failures.push('beta readiness must remain 12/12 verified_complete');\nfor(const token of ['Finalized frontend baseline: **v787**','Current live frontend at freeze: **v787**','${releaseMerge}','Firefox and WebKit','zero wrong','/familie/','subresource requests','v787 is the stable finished baseline']) if(!doc.includes(token)) failures.push('finalized state missing: '+token);\nif(failures.length){console.error('v787 finalized baseline regression failed:');for(const f of failures) console.error('- '+f);process.exit(1);}\nconsole.log('v787 finalized baseline PASS: live v787, 12/12 readiness, zero armed writes, release ${releaseMerge}, cross-engine Family alias closure recorded.');\n`;
fs.writeFileSync('check-finalized-baseline-v787.mjs',baselineGuard);

const liveGuard=`#!/usr/bin/env node\nimport fs from 'node:fs';\nconst rootN=Number(fs.readFileSync('VERSION','utf8').trim().match(/^v(\\d+)$/)?.[1]||0);\nif(rootN<787){console.log('v787 live Family evidence guard pre-v787 skip');process.exit(0);}\nconst failures=[];\nconst readiness=fs.readFileSync('beta-readiness.json','utf8');\nconst doc=fs.readFileSync('FINALIZED_PROJECT_STATE.md','utf8');\nconst combined=readiness+'\\n'+doc;\nfor(const token of ['2026-08-11','live v787','Firefox','WebKit','16','zero wrong /familie/ subresource requests','35-route','performance']) if(!combined.includes(token)) failures.push('durable v787 live evidence missing: '+token);\nif(failures.length){console.error('v787 live Family evidence regression failed:');for(const f of failures) console.error('- '+f);process.exit(1);}\nconsole.log('v787 live Family evidence PASS: public closure, Firefox/WebKit 16-case alias proof and zero wrong /familie/ requests remain durably recorded.');\n`;
fs.writeFileSync('check-family-redirect-live-evidence-v787.mjs',liveGuard);

const pkg=JSON.parse(fs.readFileSync('package.json','utf8'));
for(const cmd of ['node check-finalized-baseline-v787.mjs','node check-family-redirect-live-evidence-v787.mjs']) if(!String(pkg.scripts?.['verify:static']||'').includes(cmd)) pkg.scripts['verify:static'] += ' && '+cmd;
fs.writeFileSync('package.json',JSON.stringify(pkg,null,2)+'\n');

for(const path of [
  'scripts/v787-live-family-alias-proof.mjs',
  '.github/workflows/v787-live-proof.yml',
  'scripts/finalize-v787-live-evidence.mjs',
  '.github/workflows/finalize-v787-live-evidence.yml'
]) fs.rmSync(path,{force:true});

execFileSync('npm',['run','verify'],{stdio:'inherit'});
execFileSync('git',['config','core.whitespace','cr-at-eol'],{stdio:'inherit'});
execFileSync('git',['diff','--check'],{stdio:'inherit'});
console.log('V787_LIVE_EVIDENCE_FINALIZE=PASS');
