GEJAST patch v414

Files included
- login.html
- gejast-config.js

What changed
- Keeps the v412 and v413 login fixes.
- Simplifies the login entry page visual layer further.
- Removes the heavy decorative scene from login.html.
- Replaces the login background image with the lighter shared site background.
- Removes the fixed background attachment from login.html.
- Bumps the shared visible version from v413 to v414.

Why this patch exists
- The reported symptom changed from a hard boot break to: "I can click once on the login page but then it freezes."
- That pattern is consistent with a fragile entry page render path on interaction/focus, especially on mobile browsers.
- This patch strips the login page down further so the public entry surface behaves more like the simpler working home page.

SQL
- None required.
