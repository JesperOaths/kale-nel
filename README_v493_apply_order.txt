v493 apply order

1. Upload the files from this flat zip.
2. Run the separate SQL file:
   gejast_v493_pikken_leave_destroy_and_home_fix.sql

What changed
- home.html no longer hard-forces an old __bust version into the URL
- home.html now points to the shared gejast-config version line
- pikken_live.html rebuilt into a real actor/viewer live surface with compact seat ring, current bid, status chips, reveal cards, and bottom action dock
- mobile live page strips unnecessary explanatory text so the space is used for match info
- participants now get a leave game button
- hosts now get a stop & remove button
